function oddOrEven(array) {
  
  const sum = array.reduce((previousValue, currentValue) =>  previousValue+currentValue, 0);
  if (array == [] || sum == 0 || sum%2==0){
    return "even"
  }  else {
    return 'odd'
  }                                             
}